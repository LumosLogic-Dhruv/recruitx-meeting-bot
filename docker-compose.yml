services:
  recruitx:
    build: .
    image: recruitx:latest
    container_name: recruitx
    restart: unless-stopped
    ports:
      - "127.0.0.1:3090:80"   # Traefik proxies meet.recruitx-ai.com → this port
    env_file:
      - .env
